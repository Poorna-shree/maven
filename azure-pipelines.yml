trigger:
- master

pool:
  name: Default  # Uses any available self-hosted agent in the Default pool

steps:
- script: echo Hello 
  displayName: 'Run an echo command'
- script: mvn clean install
  displayName: 'Build with Maven'
- script: java - jar F:\poornashree\target\poornashree-0.0.1-SNAPSHOT.jar
  displayName: 'Run a jar file'